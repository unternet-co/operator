{
  "name": "@unternet/operator",
  "private": true,
  "workspaces": [
    "kernel",
    "web"
  ],
  "scripts": {
    "build:web": "cd web && npm run build && cp -r dist/ ../dist",
    "dev:web": "cd web && npm run dev",
    "dev:desktop": "concurrently 'cd desktop && npm run dev' 'npm run dev:web'"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
