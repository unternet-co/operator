{
  "name": "@unternet/operator",
  "private": true,
  "workspaces": [
    "kernel",
    "web",
    "shell"
  ],
  "scripts": {
    "build:web": "cd web && npm run build && cp -r dist/ ../dist",
    "dev:web": "cd web && npm run dev"
  }
}
